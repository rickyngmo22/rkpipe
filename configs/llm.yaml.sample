# LLM 复查配置：复制为 llm.yaml 放在 daemon 工作目录（或 RKPIPE_LLM_CONFIG 指定路径）。
# 不存在该文件 = LLM 复查禁用（provider_mock 兜底），功能不受影响。
# api_key 建议走环境变量，不要写进本文件（参见 README 安全说明）。
provider: mock            # mock | cloud
# model: gpt-4o-mini
# base_url: https://api.example.com/v1
# api_key_env: RKPIPE_LLM_API_KEY
